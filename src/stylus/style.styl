@import 'base';

$font-size = 1em

body
  font-size: $font-size;

h2
  background linear-gradient(-30deg, blue, purple, red)
  display inline-block
  font-size $font-size * 2

*
  animation-name fade
  animation-duration 2s

@keyframes fade
  from
    opacity 0
  to
    opacity 1
